package FinalProject;

import java.util.ArrayList;

// Player class - probably just going to largely handle the inventory as a text-based game shouldn't require much else
public class Player {

	// ty GPT for syntax
	// Dunno if this is how I'll handle the inventory exactly but it'll definitely be in Player
	@SuppressWarnings("unused")
	private ArrayList<Item> inventory;
	
}
